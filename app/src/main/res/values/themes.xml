<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyCalculator" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
<!--        <item name="background">@color/my_light_primary</item>-->
    </style>

    <style name="ActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <!--        <item name="titleTextStyle">@style/ActionBarTitleTextStyle</item>-->
        <item name="background">@color/my_light_primary</item>
    </style>

    <style name="Theme.APISplashScreen" parent="Theme.SplashScreen">
        <item name="android:windowSplashScreenBackground">@color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/icon_calculator</item>
        <item name="android:windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.MyCalculator</item>
    </style>

    <style name="Theme.MyCalculator" parent="Base.Theme.MyCalculator" />
</resources>